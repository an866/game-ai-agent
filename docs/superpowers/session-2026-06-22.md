# 会话记录 — 2026-06-22

## 总览

| 类别 | 内容 |
|------|------|
| LLM 模型 | deepseek-v4-flash / deepseek-v4-pro 双层切换 |
| VS Code | launch.json + settings.json 运行配置 |
| UI 改进 | 6 页面全面升级，15 任务 16 提交 |
| 联网搜索 | DDG+Tavily 回退，LangChain 工具 + MCP Server |
| 错误沉淀 | error-recovery 技能，8 类常见错误 |
| 测试 | 鸣潮/红色沙漠分层测试，发现问题并修复 |

---

## 1. LLM 模型配置

**改动：** `.env` + `config/settings.py`

| Agent | 模型 |
|-------|------|
| Router / News / General | `deepseek-v4-flash` |
| Query / Price / Recommend | `deepseek-v4-pro` |

---

## 2. VS Code 运行配置

**新增：** `.vscode/launch.json` + `.vscode/settings.json`

解决 VS Code 直接运行报 `ModuleNotFoundError` 的问题。

关键配置：
- `python.terminal.executeInFileDir: false`
- `PYTHONPATH: ${workspaceFolder}`

---

## 3. UI 全面改进

**设计文档：** `docs/superpowers/specs/2026-06-22-ui-improvement-design.md`
**实施计划：** `docs/superpowers/plans/2026-06-22-ui-improvement.md`

### 批次 A — 首页数据对接
- 指标卡对接 DB 数据（watchlist + alerts 计数）
- ChromaDB 文档计数 + CheapShark 最低折扣
- 热门游戏 Steam 实时在线人数

### 批次 B — 搜索 + 推荐修复
- RAWG 工具增加 platforms/genres 参数
- 搜索页筛选器实际生效
- 搜索页 expander 内联详情
- 推荐页偏好匹配排序 + 详情展开

### 批次 C — 新闻 + 价格监控
- Retriever game_filter + days_filter
- 价格监控 run_async_safe 替代 asyncio.run
- 删除操作 st.dialog 确认
- 新闻页 Tab 布局 + 筛选生效 + RSS 降级

### 批次 D — 对话 + 全局统一
- 统一 loading + error 组件
- 对话页多会话历史侧边栏
- 全局应用统一组件

---

## 4. 联网搜索

**设计文档：** `docs/superpowers/specs/2026-06-22-web-search-design.md`

### 架构
```
WebSearchTool
  ├─ DDG HTML (8s, 免费)
  └─ Tavily SDK (10s, 回退)
```

### 实现层
| 层 | 文件 | 用途 |
|----|------|------|
| LangChain 工具 | `src/tools/web_search.py` | Query Agent 调用 |
| MCP Server | `src/mcp/web_search_server.py` | 跨项目复用 |
| 配置 | `.claude/mcp.json` | Claude Code 注册 |

### General Chat 修复
AI 对话页从纯 LLM 改为 ReAct Agent + WebSearchTool，支持联网搜索。

---

## 5. 错误恢复技能

**位置：** `.claude/skills/error-recovery/`

| 错误 | 参考 |
|------|------|
| `ModuleNotFoundError` | python-path.md |
| GateGuard denial | gateguard.md |
| `UnicodeEncodeError: gbk` | encoding.md |
| `401 Unauthorized` | api-401.md |
| asyncio.run 冲突 | async-conflict.md |
| DDG 反爬 | ddg-fallback.md |
| Write 被分类器拒绝 | tool-blocked.md |
| 中文搜索 0 结果 | chinese-search.md |

---

## 6. 测试发现

| 测试项 | 结果 | 问题 |
|--------|------|------|
| Steam 中文搜索 | ❌ | 鸣潮/红色沙漠均 0 结果 |
| RAWG 中文搜索 | ❌ | 部分字符匹配，不相关 |
| Steam 在线人数 | ✅ | CS2: 1,461,148 |
| CheapShark | ✅ | 10 条优惠 |
| ChromaDB | ✅ | 正常加载 |
| MySQL | ✅ | 正常连接 |
| Router 路由 | ✅ | 正确 game_query |
| Tavily 搜索 | ✅ | Wikipedia + 官方 |
| DDG 搜索 | ❌ | 反爬验证页 |

### 修复
- Query prompt 中→英翻译回退
- General Chat 接入 WebSearch ReAct Agent
- Tavily 作为搜索回退链

---

## 文件变更

**新增文件：**
.env 配置模型和 Key 更新 | VS Code 配置 | MCP 配置 | 设计文档 × 3
联网搜索工具 (LangChain + MCP) | 统一组件 × 2 | 错误技能 9 文件 |
测试脚本 | 页面迁移至 _pages/

**修改文件：**
配置层 (settings.py, agents.yaml) | Agent 层 (graph.py, query.py)
工具层 (rawg.py, retriever.py) | UI 层 (app.py, session_state.py, 6 页面)

**新增依赖：** playwright, tavily-python
